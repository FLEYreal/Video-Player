// Variables
export const controlsFadeTime = 0.35; // Amount of time it takes for controls to fade on hide & show (doesn't affect timeline)
export const controlsDelay = 0.4 // Delay until controls is able to switch state, made so animation wouldn't glitch
export const buttonColor = '#dddddd' // Color of item in player
export const buttonHighlightColor = '#ffffff' // Color of hovered item
export const fullScreenDelay = 150; // Delay until fullscreen is able to switch state in milleseconds
